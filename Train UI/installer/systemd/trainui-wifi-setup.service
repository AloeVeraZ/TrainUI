[Unit]
Description=TrainUI automatic Wi-Fi setup hotspot and web portal
After=NetworkManager.service
Wants=NetworkManager.service

[Service]
Type=simple
ExecStart=/usr/local/sbin/trainui-wifi-setup watch --timeout 30 --port 80
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
