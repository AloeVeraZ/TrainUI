[Manager]
# Reboot automatically if the kernel/system manager stops responding. The
# launcher handles an application-only freeze before this watchdog is needed.
RuntimeWatchdogSec=2min
RebootWatchdogSec=10min
